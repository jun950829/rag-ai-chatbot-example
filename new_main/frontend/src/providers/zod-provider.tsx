"use client"

// ZodLocaleProvider wires locale-aware error messages when form validation is added.
// Extend this with z.config() / custom error maps once validation schemas are in place.
export function ZodLocaleProvider({ locale: _locale }: { locale: string }) {
  return null
}
